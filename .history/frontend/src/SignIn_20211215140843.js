import React from 'react';
import { AppBar, CssBaseline, Toolbar } from '@mui/material';

const SignIn = () => {
    return (
        <div>
            <CssBaseline />
            <AppBar>
                <Toolbar>
                    
                </Toolbar>
            </AppBar>
        </div>
    );
};

export default SignIn;